/**
 * Tier / scope dots — hidden app-wide (kept as a stub so call sites stay stable).
 */
export function ComplexityDots(_props: {
  filled: number;
  size: 'pill' | 'bar' | 'dot';
  className?: string;
  'aria-label'?: string;
  'aria-hidden'?: boolean;
}) {
  return null;
}
